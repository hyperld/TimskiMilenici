/**
 * Owner dashboard (and other screens): header notification control.
 * Same behavior and UI as the home header bell + sliding panel.
 */
import NotificationHeaderButton from '../NotificationHeaderButton/NotificationHeaderButton';

const NotificationWidget = NotificationHeaderButton;

export default NotificationWidget;
